import React from "react";

function SortAttack() {
  return (
    <div>
      <button>Sort by Attack Force</button>
      {/* <button>{`Sort by Attack ${
        sortOrder === "asc" ? "(low to high)" : "(high to low)"
      }`}</button> */}
    </div>
  );
}

export default SortAttack;
