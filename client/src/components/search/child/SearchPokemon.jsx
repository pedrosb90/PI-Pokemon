import React from "react";

function SearchPokemon() {
  return (
    <div>
      <select>
        <option>--Pokemon Name.. --</option>
      </select>
      <button>Reset</button>
      <button>Find</button>
    </div>
  );
}

export default SearchPokemon;
